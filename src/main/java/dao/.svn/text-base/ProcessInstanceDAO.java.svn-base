package dao;

import domain.ProcessInstance;

/**
 * Data access interface for process instances.
 *
 */
public interface ProcessInstanceDAO {
	/**
	 * Persist an instance of a process.
	 * @param pi Process instance to persist.
	 */
	void persist(ProcessInstance pi);
}
